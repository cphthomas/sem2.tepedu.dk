[build]
  command = "mkdir -p .publish && rsync -a --exclude '.git' --exclude '.publish' --exclude '.netlify' --exclude 'images' --exclude 'ignore' --exclude '.DS_Store' ./ .publish/"
  publish = ".publish"
